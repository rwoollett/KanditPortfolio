<div class="container portfolio-container">
  <div class="row g-5">
    <div class="col-md-8">
      <%= image_tag portfolio_img(@portfolio_item.main_image_url, 'main'), width: "100%" %> 
    </div>

    <div class="col-md-4">
      <h1><%= @portfolio_item.title %></h1>
      <hr/>

      <em><%= @portfolio_item.subtitle %></em>
      <p><%= @portfolio_item.body %>

      <% if @portfolio_item.technologies.count > 0 %>
        <hr/>
        <h3>Technologies Used:</h3> 
        <% @portfolio_item.technologies.each do |t| %>
          <p> <%= t.name %></p>
        <% end %>
      <% end %>

      <% if logged_in?(:site_admin) %>
      <div class="d-grid gap-2 mb-3">
        <%= link_to "Edit Item", edit_portfolio_path(@portfolio_item), class: "btn btn-warning" %>
      </div>
      <% end %>

    </div>
  </div>
</div>

